import { Options } from "../../type"
import { debug } from "../../function"
import { Client } from "discord.js"

export function button_handler(data: Options["button"], client: Client) {
    debug.warn("Button handler is not implemented yet")
}
